<h1>All artists</h1>

<br>
<%= form_tag artists_path, method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Search by Name / Genre / Location"
  %>
<% end %>
<br>

<%= render "share/banner" %>

<div class="row">
  <% @artists.each do | artist | %>
  <div class="col-xs-12 col-sm-6 col-lg-4">
    <div class="card" style="background-image: linear-gradient(rgba(0,0,0,-2.8), rgba(0,0,0,0.7)), url('<%= artist.photo.url.nil? ? 'https://images.unsplash.com/photo-1510915361894-db8b60106cb1?ixlib=rb-0.3.5&s=c3ca870aabdda474d2179770e70d356e&auto=format&fit=crop&w=1950&q=80' : cl_image_path(artist.photo) %>');">
    </div>
    <div class="card-category">
      <%= artist.genre %>
    </div>
    <div class="card-description">
      <h2>
      <%= link_to artist.name, artist_path(artist) %>
      </h2>
    </div>
    <div class="card-price">
      € <%= artist.price %>
    </div>
    <div>
      <img class="card-user" src="https://kitt.lewagon.com/placeholder/users/tgenaitay">
      <a class="card-link" href="#" >
      </a>
    </div>
  </div>
 <% end %>
</div>

<%= link_to 'All Artists', artists_path %> |
<%= link_to 'Home', root_path %>
